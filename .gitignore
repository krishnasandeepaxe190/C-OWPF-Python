# Only the Python translation is tracked/pushed to GitHub.
# The MATLAB source and manuscript documents stay local.
/Matlab/
# IEEE Access manuscript sources (under revision -- do not publish to the public repo)
/Titlelabel1/

# Python runtime artifacts
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.env
.ipynb_checkpoints/

# Solver / run logs and outputs
*.log
outputs/
Python/outputs/
Python/results/

# epyt / EPANET runtime temp artifacts
*_temp.inp
*_temp.txt
*.rpt
*.bin

# Source archives (extracted into data/)
*.zip

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# cygwin crash dumps
*.stackdump

# solver licenses (MOSEK / Gurobi) -- never commit
*.lic

# Claude Code local config -- never publish
.claude/
